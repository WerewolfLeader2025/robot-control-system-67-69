# Code Review Feedback (Please Address)

1. **Variable naming concerns:** Can `MAX_SPEED` be configurable dynamically rather than hardcoded in `config.py`?
2. **Missing tests:** Please add unit tests for edge cases in `apply_speed` (e.g., negative speeds).
3. **Documentation requests:** The `calibrate` function in `sensors.py` is undocumented. Please add a docstring.
